import { Title } from '@components/UI';
import Template from '@pages/Template';

function Quests() {
  return (
    <Template className="before:h-[290px] after:h-[270px] bg-[url('@assets/img/bg/quests.png')]">
      <Title>Quests</Title>
    </Template>
  );
}

export default Quests;
