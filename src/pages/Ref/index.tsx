import { Title } from '@components/UI';
import Template from '@pages/Template';

function Ref() {
  return (
    <Template className="before:h-[290px] after:h-[367px] bg-[url('@assets/img/bg/ref.png')]">
      <Title>Ref</Title>
    </Template>
  );
}

export default Ref;
