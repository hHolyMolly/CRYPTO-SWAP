import { Title } from '@components/UI';
import Template from '@pages/Template';

function Boost() {
  return (
    <Template className="before:h-[290px] after:h-[270px] bg-[url('@assets/img/bg/boost.png')]">
      <Title>Boost</Title>
    </Template>
  );
}

export default Boost;
